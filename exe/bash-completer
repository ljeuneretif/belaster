
function join_by
{
  local d=${1-} f=${2-}
  if shift 2; then
    printf %s "$f" "${@/#/$d}"
  fi
}

function _belaster
{
	local IFS=$'\n'

	COMPREPLY=( $(bash-completer-helper-for-belaster $COMP_CWORD $(join_by $'\n' "${COMP_WORDS[@]}")) )

	return 0
}

complete -F _belaster belaster
